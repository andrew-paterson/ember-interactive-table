import buildRequestParams from 'dummy/utils/build-request-params';
import { module, test } from 'qunit';

module('Unit | Utility | build-request-params', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = buildRequestParams();
    assert.ok(result);
  });
});
